{
  "name": "server",
  "version": "1.0.0",
  "description": "API for Contact App",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "author": "Arjun",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
